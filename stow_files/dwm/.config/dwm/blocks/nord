#!/bin/sh

nord_status="$(nordvpn status | awk '$3 ~ /Status:/ {print $4}')"

if [[ $nord_status == "Connected" ]]; then
	nord_server="$(nordvpn status | awk '$3 ~ /nordvpn.com/ {print $3}')"
    echo -ne "^c#ffffff^ ^d^${nord_server}"
fi
